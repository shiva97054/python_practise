Prime numbres :
---------------------

*) Prime numbers are important creaters

 They are building the blocks of whole numbers and central to the field known as "Number Theory"
 
 *) Prime numbers are also like ---- RSA algorithims
 but identifying prime numbers is difficult
 
 Q) What is prime number ?
 
 Only divisible by itself and 1 
   prime numbeers are (2,3,5,7,11,13,17,19 ....)  
   
   suppose ex: # Test all divisiors from 2 through n-1 (skip 1 and n)
   
   
   
 ------------------------------------------------  
   
   def is_prime(num):
    if num <= 1:
        return False  # Numbers less than or equal to 1 are not prime
    for i in range(2, int(num**0.5) + 1):
        if num % i == 0:
            return False             # Found a divisor, so not prime
    return True                      # No divisors found, so it's prime

# Example usage

number = int(input("Enter a number: "))
if is_prime(number):
    print(f"{number} is a prime number.")
else:
    print(f"{number} is not a prime number.")

   
